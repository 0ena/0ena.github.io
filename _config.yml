title: Nassos Moschos
description: "Nassos Moschos"
sourcecode: "https://github.com/0ena/0ena.github.io"

permalink: blog/:year/:month/:day/:title
exclude: ["Makefile", "README.md", "vendor"]

url: "https://0ena.github.io/"     # the base hostname & protocol for your site
baseurl: ""                   # the subpath of your site, e.g. /blog

title_separator: "•"

# Build settings
markdown: kramdown
highlighter: rouge

# Settings for analytics helper
# Set 'provider' to the analytics provider you want to use.
# Set 'provider' to false to turn analytics off globally.
#
analytics:
    provider:
    # statcounter:
    #     sc_project  :
    #     sc_security :
    #     sc_invisible: 1
    #     sc_text     : 2
    google:
        tracking_id: 'G-GX8B77859C'
    # getclicky:
    #   site_id:
    # mixpanel:
    #     token: ''
    # piwik:
    #     baseURL: '' # Piwik installation address (without protocol)
    #     idsite: ''  # the id of the site on Piwik
